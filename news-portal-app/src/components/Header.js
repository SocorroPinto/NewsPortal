import React from "react";
import Search from "./Search";

const Header = (props) => {
  return (
    <div>
      <h1>Daily Planet</h1>
      <button>SUSCRIBE NOW</button>
      <button>LOGIN</button>
    </div>
  );
};

export default Header;
